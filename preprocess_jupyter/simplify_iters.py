        # if len(syllablist) == 2:
        #     #store first and second syllale output from varbamorf
        #     first = syllablist[0]
        #     second = syllablist[1]
            
        #     ortho1 = first.get('syllable')
        #     q1 = first.get('quantity')
        #     a1 = first.get('accent')

        #     ortho2 = second.get('syllable')
        #     q2 = second.get('quantity')
        #     a2 = second.get('accent')

            
        #     #get annotations from each syll's respective analysis tiers (if any)
        #     tmp1 = firstSyll.get_annotations_between_timepoints(onset,offset)
        #     tmp2 = secSyll.get_annotations_between_timepoints(onset,offset)
        #     #if analysis tier is not empty, get measurements: 
        #     if len(tmp1) != 0:
        #         seg1 = tmp1.text
        #         von1 = tmp1.start_time
        #         voff1 = tmp1.end_time 
        #         dur = von1 - voff1
        #         mid1 = von1 + (dur/2)

            
        #     if len(tmp2) != 0:

           

            # foot = (first,second)
            


            #   for vowel in tmpinterval:
              
            #     segment = vowel.text
            #     vOnset = vowel.start_time
            #     vOffset = vowel.end_time
            #     vMidpoint = vOffset - (dur/2)
            #     dur = vOffset-vOnset
            #     tmpick = ictus.get_annotations_by_time(vMidpoint)
            #     if len(tmpick) > 0 :
            #         ick = tmpick[0].text
            #     else: ick = "off"
            #     row = (word,ortho,i,segment,q,a,ick,dur,vMidpoint) 
            #     segments.append(row)

        
            


